---
- hosts:
    - ses-nodes
  gather_facts: no
  vars:
    - domname: qalab 
  remote_user: root
  tasks:
      - name: set hostname 
        shell: |
            hostnamectl set-hostname {{ ansible_hostname }}.{{ domname }}
            cat /etc/hostname
        register: out

      - debug: var=out.stdout_lines

